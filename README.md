# Amplimentor Static Pages

This project contains the static frontend pages for **Amplimentor**, built using HTML and CSS. These pages are designed as basic UI templates without any backend integration.
